const sum1 = (2, 4) => a + b;

const sum2 = (5, 2) => a + b;

const subtract = (multiply, 2) => a - b;


const multiply = (sum1, sum2) => a * b;


const divide = (subtract, 5) => a / b;


const log = function (value) {
  console.log(value);
}

console.log(divide);